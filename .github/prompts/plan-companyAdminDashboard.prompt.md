# 公司管理后台 - 共享行动备忘录优化计划

## 已完成的工作

### 1. 环境配置
- ✅ 重新安装 pnpm 依赖（254个包）
- ✅ 配置 `.env.local` 环境变量
- ✅ 修复 `pnpm clean` 脚本 PowerShell 兼容性问题（`$pid` → `$procId`）
- ✅ 构建验证通过

### 2. 登录功能
- ✅ 认证存储从 `localStorage` 改为 `sessionStorage`
- ✅ 每次打开浏览器显示登录页，关闭浏览器后 session 自动清除
- ✅ 三个演示账号：张三(zhangsan)、李四(lisi)、王五(wangwu)，密码均为 123456

### 3. 外部系统嵌入
- ✅ 将 `工作计划助手.html` 和 `凯格咨询客户管理系统.html` 复制到 `assets/` 目录
- ✅ 侧边栏 "External Systems" 区域点击可在新标签页打开

### 4. 日历看板优化
- ✅ 事件添加 `month` 字段，12个月份都有数据
- ✅ `getEventsForDay` 按月份+日期过滤
- ✅ 日历格子最小高度 `minmax(80px, 1fr)`，事件不被截断
- ✅ 事件数量上限从3提高到4，超出可滚动查看
- ✅ 优先级背景色简化为3种：高(红)、中(琥珀)、低(蓝)
- ✅ 图例移至顶部导航按钮旁边

### 5. 团队便签置顶修复
- ✅ 新增 `sortNotes` 排序函数：置顶优先 → 时间倒序
- ✅ 所有数据操作统一使用排序函数

---

## 待优化：共享行动备忘录

### 当前状态
- 支持添加、编辑、删除备忘录
- 支持高/中/低优先级筛选
- 支持6种背景颜色选择
- 按优先级排序显示

### 可能的改进方向

#### A. 交互优化
- [ ] 添加置顶功能（类似团队便签）
- [ ] 支持拖拽排序
- [ ] 添加完成状态标记（checkbox）
- [ ] 支持批量删除

#### B. 功能增强
- [ ] 添加截止日期字段
- [ ] 添加负责人指派
- [ ] 添加标签/分类系统
- [ ] 支持搜索功能
- [ ] 添加提醒通知

#### C. 视觉优化
- [ ] 简化颜色选择（统一为3种优先级色）
- [ ] 添加动画过渡效果
- [ ] 优化移动端适配
- [ ] 添加空状态插图

#### D. 数据管理
- [ ] 支持导出为 Excel/HTML
- [ ] 添加数据统计面板
- [ ] 支持数据同步到云端

---

## 下一步行动
1. 确认用户具体想要的修改方向
2. 实现选定的优化功能
3. 测试验证
4. 部署上线
